[package]
name = "ctrl-space-wsl"
version = "0.2.0"
edition = "2021"

[dependencies]
eframe = "0.31"
fuzzy-matcher = "0.3"
toml = "0.8"
serde = { version = "1", features = ["derive"] }
dirs = "5"
libc = "0.2"

[profile.release]
lto = true
codegen-units = 1
strip = true
